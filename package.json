{
  "name": "envasadora-170g-intelligence-center",
  "private": true,
  "version": "0.4.0-demo",
  "type": "module",
  "scripts": {
    "prepare": "node scripts/assemble-app.mjs",
    "dev": "npm run prepare && vite",
    "build": "npm run prepare && vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "lucide-react": "^0.468.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "recharts": "^2.15.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.5"
  },
  "engines": {
    "node": ">=20"
  }
}
